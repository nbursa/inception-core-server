# Required: UUID of an existing ChromaDB collection
CHROMA_COLLECTION_ID=your-collection-uuid

# Optional: LLM server base URL (used for fallback responses)
LLM_URL=http://llm:11434

# Optional environment mode and logging
MCP_ENV=development
RUST_LOG=info

# Optional: ChromaDB URL (change if using external service)
CHROMADB_URL=http://chromadb:8000
